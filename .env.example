# Optional for localhost; required when accessing the dashboard remotely.
ADMIN_PASSWORD=
# Comma-separated dashboard hostnames (without scheme or port).
ALLOWED_HOSTS=localhost,127.0.0.1,[::1]
DATA_DIR=./data
# Set credentials in the dashboard. They are encrypted in the local database.

